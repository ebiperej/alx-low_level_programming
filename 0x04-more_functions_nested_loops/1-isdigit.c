#include "main.h"
/**
  * isdigit - checks for a digit
  * @c: character to be checked
  * Return: 1 if digit, 0 otherwise
  */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
	retun (1);
	}
	return (0);
}
